# Omarchy user template: herdr typography.
#
# Rendered into shell.font.toml and applied as a section override replacing
# [font]. base-size is the rem root: every Style.font.<token> derives from it,
# so bumping it scales the whole type ramp.
#
# waybar used font-size 14px.

[font]
base-size = 14
