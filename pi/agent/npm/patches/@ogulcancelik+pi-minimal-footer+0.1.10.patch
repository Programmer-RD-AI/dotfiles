diff --git a/node_modules/@ogulcancelik/pi-minimal-footer/index.ts b/node_modules/@ogulcancelik/pi-minimal-footer/index.ts
index bfb8d7c..050e20a 100644
--- a/node_modules/@ogulcancelik/pi-minimal-footer/index.ts
+++ b/node_modules/@ogulcancelik/pi-minimal-footer/index.ts
@@ -858,11 +858,9 @@ export default function (pi: ExtensionAPI) {
     return `${dim(window.label)} ${bar} ${pct}${timeStr}`;
   }
 
-  function renderUsageLine(usage: UsageSnapshot, width: number, theme: any): string[] {
+  function renderUsageSegments(usage: UsageSnapshot, width: number, theme: any): string[] {
     if (!usage.windows.length) return [];
 
-    const dim = (s: string) => theme.fg("dim", s);
-    const sep = " " + dim(">") + " ";
     const segments: string[] = [theme.fg("accent", usage.provider)];
 
     for (const w of usage.windows) {
@@ -877,7 +875,7 @@ export default function (pi: ExtensionAPI) {
       );
     }
 
-    return wrapFooterSegments(segments, width, sep);
+    return segments;
   }
 
   function getThinkingLevel(ctx: any): string {
@@ -1075,12 +1073,12 @@ export default function (pi: ExtensionAPI) {
             ]),
           ];
 
-          lines.push(...wrapFooterSegments(statusBlocks, width, sep));
-
           if (latestUsage && latestUsage.windows.length > 0) {
-            lines.push(...renderUsageLine(latestUsage, width, theme));
+            statusBlocks.push(...renderUsageSegments(latestUsage, width, theme));
           }
 
+          lines.push(...wrapFooterSegments(statusBlocks, width, sep));
+
           return lines.map((line) => truncateToWidth(line, width));
         },
       };
